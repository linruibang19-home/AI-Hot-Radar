# AI Hot Radar 当前交接（更新至 2026-08-17 CST）

这是判断当前代码、生产版本、数据和下一任务的唯一入口。累计历史见 `project-status.md`，
旧交接已移入 `../history/`，逐轮 RAG 实验见 `../eval/`，压测证据见 `../loadtest/`。

## 1. 当前生产基线

- 在线地址：`https://aihotradar.online`；
- 当前生产发布为 `v0.1.18`，提交
  `02e2d83b5f7794b0ee4f6fc4193a8ff6f0cfb935`；生产负载证据仍绑定
  `ba5cfbeb1c1bd581cd95f4fa5c761fd1bb8bfc22` / `v0.1.12`，因为 TASK-M5-021 只收口后端
  分层与运行文档，没有改变公开 API、数据库或 RAG 策略；
- 服务器 `/opt/ai-hot-radar` 的 `main` 分支、`IMAGE_TAG` 与 web/core-api/ai-service
  三张镜像在每次发布时精确对齐同一提交，工作树保持干净；
- 香港 Ubuntu 22.04.5、2C4G、40 GiB；Caddy 对外只暴露 80/443，业务 API、PostgreSQL、
  Redis 仅在 Compose 网络内；
- 10 个容器健康；preflight、公开 smoke、GitHub 全量 CI 和 GHCR 三镜像构建通过；
- 生产 `.env` 仅在目标机且权限 600，不进入仓库、日志或文档。

## 2. 生产数据快照

2026-08-17 发布后从 PostgreSQL 当前 schema 实查：

| 指标 | 数值 |
|---|---:|
| 信源 / configured enabled / runtime ACTIVE | 143 / 131 / 110 |
| 内容 | 2617 |
| active chunk / 已向量化 | 11700 / 11700 |
| Story | 2142 |
| RAG 问答 / 引用 | 232 / 990 |
| 报告 | 21 |
| Flyway | V026 |

PostgreSQL 是内容、chunk、Story、报告、订阅、答案、引用与用量事实源。Redis 只负责可重建的
Spring 热读缓存、RAG embedding/答案/会话热副本/近似索引、匿名限流和 30 秒运行统计快照。

同日运行参数复核：宿主机可见约 3.4 GiB 内存和 2 GiB swap；Core API 容器上限 512 MiB，
Java 21 进程以 `-XX:MaxRAMPercentage=75` 启动，同参数估算最大 heap 371.25 MiB。广州
Ubuntu 24.04 仍是 ICP 备案完成后的迁移目标，不是当前生产环境。

`v0.1.18` 部署前，backup worker 生成并通过 catalog/SHA256 校验的 183 MiB 备份；同一备份在
隔离库恢复出 `140|2611|11746|2137|21|026`，分别对应 source/content/chunk/story/report/Flyway，
恢复库随后自动删除。恢复快照与上表当前读数之间的增长来自备份后的持续采集，不是数据丢失。

## 3. TASK-M5-020/021 生产验证

RAG `/rag/stats` 原先在 async route 内重复执行同步 psycopg 聚合；修复为 `to_thread`、按窗口
single-flight 和 Redis 30 秒 fail-open 快照。香港生产 1→2→5 VU、60 秒内部只读验证完成
1536 HTTP 请求、25.09 req/s、0 错误；AI/Core/Web P95 为 10.69/96.82/644.09 ms。
PostgreSQL 内容流/RAG 统计组件为 741.02/79.43 TPS、0 失败；Redis PING 40.4k–45.4k req/s、
0 eviction。完整环境、冷热语义和资源快照见
[`../loadtest/2026-08-14-m5-020-production.md`](../loadtest/2026-08-14-m5-020-production.md)。

这些数字是发布验证，不是最大容量：业务 HTTP、代表 SQL 与 Redis PING 的口径不能互换。
容量上限仍需同规格隔离副本、恒定到达率阶梯和 1–2 小时 soak。

TASK-M5-021 通过 PR #22、主分支 CI、`v0.1.14` Release、同 SHA 三镜像、生产 preflight、
10 容器健康与公开 smoke 完成闭环。Java Controller→JDBC 与 Python FastAPI 领域渗透均有
架构回归门禁；它是可维护性收口，不把目录重排包装成吞吐提升。

TASK-M5-024/025 通过 PR #26、主分支 CI、`v0.1.15` Release 与同 SHA 生产部署完成闭环。
公开 `/items`、日期导航和分类计数现在只读取 `ENRICHED` 且中文标题/摘要完整的记录；原始英文
正文仍保留在 revision/chunk 证据层。新增三份 handbook 分别解释异构信源入库与切块、Redis
key/TTL/失效边界，以及 90 题黄金集到 `/eval` 质量页的生成和版本化过程。发布后公网 smoke
覆盖 `/health`、首页、动态、报告、问答、RAG 质量、运行状态、robots、sitemap 与管理边界。

TASK-M1-002 的第二批国内官方来源通过 PR #30、`v0.1.17` Release 与同 SHA 生产部署完成。
阿里云官方 AI 博客、腾讯云 AI 团队各做 3 篇受控生产 canary，6/6 回源全文通过、6/6 完成
结构化，共形成 37 个活跃证据块并 37/37 向量化。一次超出 25 个实体上限的模型输出被 Pydantic
schema 门禁拦截并经限定修复重试成功；没有绕过校验直接入库。华为聚合入口因缺少稳定同站官方
文章流继续关闭。完整证据见
[`../product/domestic-source-expansion-20260817.md`](../product/domestic-source-expansion-20260817.md)。

TASK-M5-026 通过 PR #32、`v0.1.18` Release 与同 SHA 生产部署完成。信源后台不再使用硬编码
注册总数，而是显示当前环境、配置版本、数据库更新时间和动态汇总；刷新按钮只无缓存重读当前
数据库，不触发采集或跨环境同步。新增美团、PaddlePaddle、ModelScope 三个官方源的 5 篇生产
canary 全部进入 `ENRICHED`，形成 34 个活跃 chunk，34/34 已向量化；美团为 `ACTIVE`，后两者
在 0 连续失败的 `PROBING` 观察期。完整证据仍见上述中文信源扩展记录。

## 4. 标准发布流程

1. `codex/<task>` 单任务分支开发并完成相关门禁；
2. push、PR、GitHub CI 全绿后合并 `main`；
3. 为合并提交打 `v*` 标签，Release workflow 发布同 SHA 三镜像；
4. 服务器 `main` 仅 fast-forward，`IMAGE_TAG=sha-<HEAD>`，运行
   `infra/scripts/deploy-production.sh`；
5. 验证 HTTPS、核心页面、管理边界、RAG、备份和容器健康；失败时回退上一不可变 tag。

不要在服务器直接开发，不要上传本地 `.env`。GitHub 是代码事实源；服务器只持有生产密钥、
数据卷、备份和运行镜像选择。

## 5. 下一步

1. 在新腾讯云广州 2C4G 上做平行恢复、同 SHA smoke 与隔离阶梯/soak，备案完成后再切 DNS；
2. 观察下一期真实订阅投递并逐步迁移到有 SPF/DKIM/DMARC 的域名发件服务；
3. 扩展 RAG 实体、时间窗、别名、错别字、跨语言和同名噪声集；
4. 增加用户反馈闭环与受鉴权的管理重跑能力；
5. 每月执行异机备份恢复演练并复核供应商预算上限。
