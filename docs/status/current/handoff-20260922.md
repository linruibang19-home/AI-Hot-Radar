# 交接：当前本地与线上状态（2026-09-22）

> 新会话的冷启动输入。取代 `migration-handoff-20260917.md`——**那份里的服务器
> `47.242.229.41` 已于 2026-09-18 到期消失**，不要再按它操作。
>
> 表内数字均为 2026-09-22 连上生产实查，不是估算。

---

## 1. 一句话状态

线上正常运行，**无故障**。主线工作是 RAG 优化，方案已定但**尚未动代码**。

---

## 2. 线上环境

| 项 | 值 |
|---|---|
| 站点 | `https://aihotradar.online`（HTTP `200`） |
| 服务器 | `47.243.15.6` — **香港九龙，阿里云，AS45102**，延迟约 7ms |
| 系统 | Ubuntu 24.04.5 LTS · 2 核 · 3.7 G 内存 + 2 G swap · 39 G 磁盘（用 32%） |
| 登录 | `ssh -i ~/.ssh/ai_hot_radar_deploy_ed25519 deploy@47.243.15.6` |
| 认证 | **仅公钥**。公钥指纹 `SHA256:NXt/Qqt2xb0oDHdO9ojAXUaIN5KuLWAkyk+5X0EdmUM` |
| 项目路径 | `/opt/ai-hot-radar`（属主 `deploy`，**不要用 root 操作**） |
| 容器 | 10 个，全部健康 |
| DNS | 腾讯云 DNSPod，A 记录 `@` → `47.243.15.6`，TTL `600` |
| 防火墙 | `ufw` inactive、`iptables` 零规则、阿里云安全组已放行 22/80/443 |

> **密码不记录在此。** 本次恢复全程用公钥；`.env` 明文副本在
> `D:\Backups\AI Hot Radar\env-backup-20260919.txt`，不要提交进 git。

## 3. 版本

| | 值 |
|---|---|
| 生产 tag | **`v0.1.29`** |
| 生产 `IMAGE_TAG` | `sha-cc8bd4a5f619eed3f20fa72a2cbf502c2147d148` |
| 服务器 git HEAD | `cc8bd4a`（与 `IMAGE_TAG` 一致 ✅） |
| 本地 / `origin/main` | **`70d3960`** |

> **main 比生产超前 1 个提交，是纯文档**（PR `#66` 的文档合并）。
> 功能代码线上线下完全一致。
>
> `deploy-production.sh` 强制 `IMAGE_TAG == sha-$(git rev-parse HEAD)`，
> 所以服务器**故意停在 `cc8bd4a`**。不要为了文档去 `git pull` 服务器——
> 那会让部署脚本报 `IMAGE_TAG does not match checkout HEAD`。

## 4. 数据（2026-09-22 实查）

| content_item | content_chunk | source | story | report | 库大小 |
|---:|---:|---:|---:|---:|---:|
| **7392** | **39983** | **151** | 3450 | 61 | 1658 MB |

**信源状态**（列名是 `runtime_state`，**不是** `status`）：

```
ACTIVE=133  CONFIGURED=7  QUARANTINED=6  METADATA_ONLY=2  PROBING=2  RATE_LIMITED=1
```

## 5. 备份

| 项 | 值 |
|---|---|
| 服务器 | `/opt/ai-hot-radar/infra/compose/backups/`，每日 `pg_dump`，保留 7 天 |
| 异地 | `D:\Backups\AI Hot Radar\`，Windows 计划任务 `AI Hot Radar - Pull Production Backup` |
| 手动触发 | `docker compose ... run --rm -e BACKUP_RUN_ONCE=true backup` |
| 校验 | `restore-verify` 服务（比对 Flyway 版本 + 五张表行数） |
| 仓库分支备份 | `repo-branches-backup-20260921.bundle`（4.1 M，`verify` 通过） |

---

## 6. 本地

| 项 | 值 |
|---|---|
| 路径 | `D:\Dev\Autumn recruitment project\AI Hot Radar` |
| 分支 | `main`，`70d3960`，工作树干净 |
| 分支数 | **仅 `main`**（2026-09-21 清掉 30 个陈旧分支） |
| 大小 | `866 MB`（清理前 1510 MB） |

**本机环境的坑**（不在仓库文档里）：

- **Postgres 宿主机端口是 5433 不是 5432**（本机另装了 PostgreSQL 占着 5432，且需密码）
- 跑 Java 测试用 `maven:3.9-eclipse-temurin-21` 容器（本机 JDK 低于 21）
- 跑 Playwright 用 `mcr.microsoft.com/playwright:v1.49.1-noble`，接 `--network ai-hot-radar_default`
- **本地 pytest 过了不等于 CI 会过**：ruff / format / mypy 本地不自动跑，提交前跑全门禁
- 本机 `PostgreSQL 16` 自带 `pg_restore` / `psql`，在 `C:\Program Files\PostgreSQL\16\bin`

---

## 7. 下一步：RAG 优化（尚未开工）

方案：[`../../design/rag-optimization-plan.md`](../../design/rag-optimization-plan.md)

**基线**：241 次真实提问，总延迟 p50 **`10,794 ms`**、p95 `15,748 ms`、最慢一次 `167,351 ms`。

单次拆解的两个异常：

```
numeric_audit   6,006 ms   ← 占 ~24%，从未被评估过它防住了什么
sparse          3,010 ms   ← 2026-08-04 测的是 24 ms，涨了 125 倍
```

**第一项任务**：给 `numeric_audit` 立可证伪判据 —— 90 题跑「开/关」双变体，
被正确纠正 `< 3/90` 则删除，`≥ 3` 则改为仅在答案含数字时触发。
**两种结果都是好结果。**

后续：`sparse` 3 秒定位 → 19 题人工复核 → 三路并行 + 端到端超时 → 中文分词。

**明确不做**：MCP 接入、中文 bigram（B13 已证伪）、融合权重调参（B8 负结果）。

---

## 8. 未决事项

| 事项 | 状态 |
|---|---|
| **轮换 4 把 API Key** | **未做** —— `LLM_API_KEY` / `EMBEDDING_API_KEY` / `GITHUB_TOKEN` / `POSTGRES_PASSWORD` 在对话截图中暴露过三次 |
| **轮换服务器密码** | **未做** —— 曾在对话中出现；不影响 SSH（仅公钥） |
| 供应商消费上限 | **机主决定不设**。`.env` 中已注明是主动豁免而非已设置——目前没有任何供应商侧花费闸门 |
| 19 道黄金题人工复核 | 未做，卡着所有阈值工作 |
| `machine-heart` 等 3 个前端渲染源 | 需浏览器渲染，评估结论是**先不做**（内存不允许，且只换来 3 个 P1/P2 源） |
| 7 个被 AS45102 拦的信源 | 2026-09-19 实测仍 403/429。换服务商才可能解除 |
| `data/topic-map-review/` | 未完成的人工标注中间产物，未处置 |

---

## 9. 长期约定

- 提交**不要**带 Claude 署名或 `Co-Authored-By`，作者一律 `LRB <linruibang19@gmail.com>`
- Java 侧用 **Maven**，不用 Gradle
- 每完成一步汇报进度并写进 `docs/`
- 不以 root 操作 `/opt/ai-hot-radar`
- 明文密钥不进聊天记录、不进 git

---

## 10. 仓库目录（2026-09-22 整理）

同日做了一次目录精简：**只移动文档、契约和评测 JSON**。`apps/` 下只改了 3 处注释和
`eval-summary.json` 的 `generatedFrom` 字符串，功能代码不变，**生产无需为此重新部署**。

| 以前 | 现在 |
|---|---|
| `api/openapi.yaml`、`schemas/*.json` | `contracts/` |
| `docs/status/eval/*.json`（45 份） | `data/eval-runs/`；`/eval` 摘要由 `scripts/build_eval_summary.py` 从这里生成 |
| `docs/status/{delivery,product,operations,loadtest}/`、`eval/*.md`、架构核查 | `docs/status/evidence/`，索引在 `docs/status/README.md` |
| `docs/status/history/`、`docs/archive/` 的三个子目录、两份 m5 旧方案、过期交接 | `docs/archive/`（平铺） |
| `docs/design/current/` | `docs/design/` |
| `archive-policy.md` 与 7 个目录级 README | 合并进 `docs/README.md` 与 `docs/status/README.md` |

**这些路径不能动**——生产 bind mount、镜像构建或本机计划任务直接引用：`config/`、`data/`、
`database/migrations/`、`infra/caddy/Caddyfile`、`infra/scripts/`、`infra/compose/`。
服务器下次部署检出新提交后，`data/` 挂载里会多出 `eval-runs/`（约 2.3 MB，只读，服务不读取）。
