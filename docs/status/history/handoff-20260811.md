# AI Hot Radar 当前交接（2026-08-11 23:06 CST）

这是当前 checkout 的继续开发与首次上线入口。完整历史在 `project-status.md`，任务状态以
`docs/spec/08-roadmap-ai-ide.md` 为准。

## 1. Git、运行与安全边界

- GitHub `main` 与发布标签 `v0.1.3`：`4c4973575249f7f1a883a36a9cf3532a14fef0a4`；
- 本轮四个独立功能提交：飞书清理/暂停部署 `2a93c9b`、邮箱订阅 `2eb929a`、
  DeepSeek 模型切换 `87f74bf`、RAG 工程页收口 `0ef8526`；
- PR #3 已合并，`v0.1.3` Release 全绿并发布三张不可变镜像；生产服务器 checkout 已
  fast-forward 到同一提交，项目容器数仍为 0；
- 本地唯一运行方式是 `docker compose -f infra/compose/docker-compose.yml ...`；
- 不打印或提交 `.env`。聊天中出现过的 GitHub、DeepSeek、SiliconFlow 凭据均视为泄露，
  不进入生产配置；上线前必须在各控制台吊销/轮换并设置消费上限。

当前本地 PostgreSQL、Redis、Core API、AI Service、Web、pipeline、scheduler 与 Mailpit
均运行；有健康检查的服务全部 healthy。

## 2. 当前产品能力

- 140 个注册信源持续采集、全文回源、清洗、去重、结构化、精选和事件聚合；
- 日报、周报、月报经确定性发布门只公开 PUBLISHED 版本；
- 报告页支持邮箱双重确认订阅、daily/weekly/monthly 周期、IANA 时区、08:30 后定时发送、
  失败重试、幂等投递与退订；生产尚未配置真实 SMTP；
- RAG 使用 pgvector + PostgreSQL FTS 混合召回、RRF、SiliconFlow bge-reranker、
  Story 折叠、父块扩展、DeepSeek 生成、服务端引用绑定、段落支持门和安全拒答；
- DeepSeek 生成模型仅允许在 V4 Flash / V4 Pro 白名单间切换，默认当前为 Flash v3；
  SiliconFlow embedding/reranker 固定，不支持用户切换；
- `/eval` 已改为“RAG 质量”，先显示发布门与风险；`/ops` 已改为“运行状态”，先显示
  SLO、价目口径、瓶颈与动作。底层实验记录全部保留。

## 3. 当前数据库快照

2026-08-11 由运行中的 PostgreSQL 实查：

| 指标 | 数值 |
|---|---:|
| 信源 / ACTIVE | 140 / 108 |
| 内容 | 1915（采集仍在增长） |
| 分块 / 已向量化 | 7264 / 7264 |
| Story / 多信源 Story | 1498 / 32 |
| 实体 | 5203 |
| RAG 问答 / 引用 | 215 / 910 |
| 报告 | 15：日报 11、周报 3、月报 1 |
| PUBLISHED 报告 | 15 |
| 正式订阅 / 投递 | 0 / 0（本地验收数据已清理） |
| Flyway | V024 success=true |
| 生成模型配置 | deepseek-v4-flash，version 3 |

`llm_usage` 已有 12 次 V024 价目快照调用；3403 次更早调用没有不可变价目，只能按
legacy fallback 回算。金额是价目估算，不是供应商账单。

## 4. RAG 当前发布证据

| 门禁 | 实测 | 判定 |
|---|---:|---|
| 主 90 题 Recall@20 | 0.8994（门槛 0.85） | 通过 |
| 中文厂商专项 Recall@20 | 0.9333 | 通过 |
| 加真实近邻噪声后 Recall@20 | 0.9333 | 无退化 |
| 句级引用完整性 | 0.9881（门槛 0.95） | 通过 |
| 段落支持达标率 | 0.9344（门槛 0.90） | 通过 |
| 可答题误拒 | 0 / 78 | 通过 |
| 诱导题错误断言 | 0 / 12 | 通过 |
| 高风险人工 P0 | 0 | 通过 |

发布 run：`B9-20260810T195839Z`、`GEN-20260810T201738Z`、
`SPECIALIST-20260810T183222Z`。自动 citation precision 只作诊断，不能替代人工 P0；
噪声结论目前是 15 题专项小样本。最近 30 天线上问答 213 次，p50/p95/p99 为
10977/15606/34804ms，p95 SLO 为 ok。

## 5. 2026-08-11 最终本地门禁

| 栈/流程 | 结果 |
|---|---|
| Spec | 140 sources / 9 profiles / 38 social targets，通过 |
| Python | Ruff、format、mypy 86 files、pytest 878 passed / 2 skipped |
| Java 21 | Maven verify；74 tests，0 failure/error/skip |
| Web | npm audit 0；typecheck、lint、71 tests、Next.js production build |
| 空库迁移 | Flyway 25 migrations，V001–V024，`024:true` |
| 当前库 | V024；Core/AI/Web healthy |
| 备份恢复 | 102 MiB `pg_dump -Fc`，SHA/目录校验与隔离恢复通过；恢复快照 `140|1915|7264|1498|15|024` |
| 页面/API | 首页、动态、精选、Story、主题、三周期报告、RAG、质量、运行、管理页、sitemap 与健康端点均 200 |
| 生产 Compose | fixture preflight 与 Compose render 通过 |

ChromeCodex 连接仍在启动浏览器前失败：`failed to write kernel assets ... (os error 3)`。
因此生产构建与 HTTP 门已通过，但新的桌面/窄屏视觉截图不能标为完成。

## 6. 生产上线当前状态

- 域名 `aihotradar.online` A 记录指向香港 Ubuntu 22.04 服务器；
- 服务器已有 deploy 用户、SSH 密钥、UFW 22/80/443、Docker、2GiB swap 和权限 600 的
  fail-closed 生产配置；
- `v0.1.3` / `sha-4c4973575249f7f1a883a36a9cf3532a14fef0a4` 的 web、ai-service、
  core-api 镜像已发布，服务器可读取 manifest；
- 真实 SMTP、专用模型凭据、provider 消费上限、HTTPS 告警与异机备份仍未闭环；
- 在这些外部闸门齐全前，生产容器继续保持停止是正确状态。

## 7. 下一步顺序

1. 在供应商控制台轮换专用低额度 key、设置消费上限；配置生产 SMTP、SPF/DKIM/DMARC；
2. 配置可实收的 HTTPS 告警通道与异机备份目标；
3. 只在目标机 SSH 会话中补齐 `.env`，运行 `preflight.sh`；
4. 预检全绿后执行 `deploy-production.sh`；
5. 公网 smoke、TLS/安全头、模型真实调用、订阅邮件、告警失败/恢复、异机备份与目标机
   隔离恢复全部实收后，TASK-M5-006 才能改为完成。

不可把“代码通过”写成“生产已上线”；不可把聊天里出现的旧 key 复用到目标机。
