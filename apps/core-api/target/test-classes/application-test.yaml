# Unit tests must not require live infrastructure. Flyway is disabled here and
# exercised for real against PostgreSQL in the Compose smoke check and CI's
# migration job, per AHR-QSO-700 §2.
spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_hot_radar_test
    username: test
    password: test
  data:
    redis:
      url: redis://localhost:6379/1
